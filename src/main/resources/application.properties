spring.application.name=slipsync
clerk.secret.key=sk_test_yFiRoC1AWUdBx25K5Hb5kAk7EUujGVo0rduTWesa7k
# --- SUPABASE DATABASE CONFIGURATION ---
# Note the "?sslmode=require" at the end, which is important for Supabase
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require

# The username MUST be exactly as it appears in your connection string
spring.datasource.username=postgres.dosugzjhdbfvwzkcecoj

# Your actual database password
spring.datasource.password=c03VLadfSCvfLjxu

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
# Turn off Hibernate schema validation/auto-DDL for now while iterating on the database schema.
spring.jpa.hibernate.ddl-auto=none

# Disable server-side prepared statements when going through Supabase PgBouncer
# to avoid errors like "bind message supplies X parameters, but prepared statement 'S_n' requires Y".
spring.datasource.hikari.data-source-properties.prepareThreshold=0
# src/main/resources/application.properties
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=60000  
spring.datasource.hikari.connection-timeout=30000


# --- LOGGING LEVELS ---
# Hide Hibernate internal logs
logging.level.org.hibernate=WARN
# Hide Hikari pool logs
logging.level.com.zaxxer.hikari=WARN
# Keep your own app logs (Controllers, etc.) at INFO
logging.level.com.slipsync=INFO